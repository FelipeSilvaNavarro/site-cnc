# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (NUNCA commitar segredos)
.env
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# claude code (configurações locais da máquina)
.claude/settings.local.json

# Cópias de segurança feitas antes de reescrever arquivo (regra do CLAUDE.md:
# inativar e guardar, nunca apagar). Ficam no disco, fora do histórico.
*.bak-antes-*
